[package]
name = "rustos"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 0
lto = false

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[dependencies]
# this is just a macro, it doesn't get included in the execuable
paste = "1.0"

